<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.library.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.library.domain.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="account" jdbcType="VARCHAR" property="account"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="mail" jdbcType="VARCHAR" property="mail"/>
        <result column="address" jdbcType="VARCHAR" property="address"/>
        <result column="type" jdbcType="INTEGER" property="type"/>
        <result column="pic" jdbcType="VARCHAR" property="pic"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,account,password,name,phone,mail,address,type,pic
    </sql>

    <insert id="insert" parameterType="com.example.library.domain.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="account != null">account,</if>
            <if test="password != null">password,</if>
            <if test="name != null">name,</if>
            <if test="phone != null">phone,</if>
            <if test="mail != null">mail,</if>
            <if test="address != null">address,</if>
            <if test="type != null">type,</if>
            <if test="pic != null">pic,</if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="account != null">#{account},</if>
            <if test="password != null">#{password},</if>
            <if test="name != null">#{name},</if>
            <if test="phone != null">#{phone},</if>
            <if test="mail != null">#{mail},</if>
            <if test="address != null">#{address},</if>
            <if test="type != null">#{type},</if>
            <if test="pic != null">#{pic},</if>
        </trim>
    </insert>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE from user
        WHERE id = #{id}
    </delete>

    <update id="update" parameterType="com.example.library.domain.User">
        update user
        <set>
            <if test="account != null">account = #{account},</if>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="mail != null">mail = #{mail},</if>
            <if test="address != null">address = #{address},</if>
            <if test="type != null">type = #{type},</if>
            <if test="pic != null">pic = #{pic},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="findall" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"></include>
        from user
    </select>

    <select id="findbyid" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List" />
        from user
        WHERE id = #{id}
    </select>

    <select id="findbyaccount" resultMap="BaseResultMap" >
        select
        <include refid="Base_Column_List" />
        from user
        WHERE account = #{account}
    </select>


    <select id="verify" resultType="java.lang.Integer">
        select count(*) from user where account=#{account} and password=#{password} and type=#{type};
    </select>

</mapper>















